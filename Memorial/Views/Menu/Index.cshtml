@model IEnumerable<Memorial.Core.Domain.Site>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@foreach (var site in Model)
{
    @Html.ActionLink(site.Name, "Catalog", new { siteId = site.Id })
    <br />
}